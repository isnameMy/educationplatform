# 🎓 EDU-Platform — Единая образовательная платформа

> **MVP для хакатона**: решает проблему разрозненных платформ — задания не теряются, обратная связь мгновенная, прогресс прозрачен.

## Описание

Этот проект представляет собой прототип **цифровой образовательной платформы**, разработанный в рамках хакатона PSB Hack&Change 2025. Цель — создать **единую среду** для обучения, объединяющую учебные материалы, задания и взаимодействие между студентами и преподавателями. Реализован **путь студента**: изучение материалов, загрузка заданий, получение комментариев и оценок.

https://github.com/user-attachments/assets/6f3b8c2a-1b0a-4b5e-9f9a-1e3a1e3e3e3e

## ✨ Возможности

- 👩‍🎓 **Студент**:  
  - Регистрация → выбор курса → выполнение заданий  
  - Загрузка файлов (`.py`, `.ipynb`, `.pdf`)  
  - Мгновенное получение оценки и фидбека  


## Технические характеристики

- **Стек**: Python, FastAPI, Jinja2, HTMX, SQLAlchemy, SQLite, Bootstrap 5
- **Архитектура**: Backend (FastAPI) + Frontend (HTML/CSS/JS с Jinja2 и HTMX)
- **Контейнеризация**: Docker, Docker Compose
- **База данных**: SQLite (локальный файл `db.sqlite`)
- **Файлы**: Загрузка файлов студентами (до 100 МБ), хранение в `uploads/`
- **Аутентификация**: Простая сессионная аутентификация (email -> роль)

## Структура проекта
.
├── README.md # Этот файл
├── docker-compose.yml # Конфигурация для запуска через Docker Compose
├── Dockerfile # Инструкции для создания Docker-образа бэкенда
├── requirements.txt # Зависимости Python
├── src/ # Основной код приложения
│ ├── main.py # Основной файл FastAPI (роуты, логика)
│ ├── database.py # Настройка подключения к БД (SQLAlchemy)
| ├── seed_data.py # Скрипт для заполнения БД начальными данными (курс "Python для анализа данных")
│ ├── models.py # Модели SQLAlchemy (User, Course, Assignment и т.д.)
│ └── jinja_filters.py # Кастомные фильтры для Jinja2 (например, from_json)
├── db.sqlite # (Игнорируется .gitignore) Локальная БД (создаётся при запуске seed_data)
├── frontend/ # Фронтенд (шаблоны, статика)
│ ├── templates/ # HTML-шаблоны Jinja2
│ │ ├── base.html # Базовый шаблон
│ │ ├── home.html # Главная страница (регистрация/вход)
│ │ ├── register.html # Шаблон выбора роли/email
│ │ ├── student/ # Шаблоны для студента
│ │ │ ├── courses.html # Страница курсов студента
│ │ │ ├── course_detail.html # Сводка по курсу
│ │ │ ├── module_base.html # Страница модуля (теория, практика, видео, тест)
│ │ │ └── module_assignment.html # Отображение задания и сабмишена (необязательно, если всё в module_base)
│ │ └── teacher/ # Шаблоны для преподавателя (если реализованы)
│ │ ├── dashboard.html # Дашборд преподавателя
│ │ └── review.html # Страница проверки работы
│ └── static/ # Статические файлы (CSS, JS, изображения, видео)
│ ├── uploads/ # (Опционально) Примеры файлов или для dev (если не в Docker volume)
│ └── videos/ # (Опционально) Примеры видео для заглушек
├── uploads/ # Папка для загрузки файлов студентами (Docker volume)

---

## Запуск проекта

### Требования

- **Python 3.9+** (рекомендуется 3.11)
- **Docker**
- **Docker Compose**

### Установка и запуск

1.  **Склонируйте репозиторий**:
    ```bash
    git clone <URL_ВАШЕГО_РЕПОЗИТОРИЯ>
    cd <НАЗВАНИЕ_ПАПКИ_РЕПО>
    ```

2.  **(Опционально, но рекомендуется) Создайте виртуальное окружение** (если запускаете локально без Docker):
    ```bash
    python -m venv venv #Windows(cmd)
    source venv/bin/activate  # Linux/macOS
    # или
    venv\Scripts\activate     # Windows
    ```

3.  **(Опционально) Установите зависимости** (если запускаете локально):
    ```bash
    pip install -r requirements.txt
    ```

4.  **Запустите проект с помощью Docker Compose**:
    ```bash
    docker-compose up --build
    ```
    > **Примечание**: `--build` пересобирает образ, если были изменения в `Dockerfile` или `requirements.txt`. В первый запуск это **обязательно**.

5.  **Инициализируйте базу данных**:
    *   После запуска `docker-compose up`, в **новом терминале** выполните:
        ```bash
        # Зайдите в запущенный контейнер
        docker exec -it <ИМЯ_КОНТЕЙНЕРА_BACKEND> bash
        # Внутри контейнера запустите скрипт
        python -m src.seed_data
        # Выйдите из контейнера
        exit
        ```
    *   **Альтернатива (если `seed_data.py` в корне)**:
        ```bash
        # Зайдите в запущенный контейнер
        docker exec -it <ИМЯ_КОНТЕЙНЕРА_BACKEND> bash
        # Внутри контейнера запустите скрипт
        python seed_data.py
        # Выйдите из контейнера
        exit
        ```

6.  **Откройте браузер** и перейдите по адресу:
    ```
    http://localhost:8000
    ```

7.  **Протестируйте функционал**:
    *   Зарегистрируйтесь как **студент**, используя email `student@test.com`.
    *   Перейдите на курс "Python для анализа данных".
    *   Просмотрите модули, посмотрите тесты, попробуйте отправить файл задания.
    *   (Если реализован преподавательский трек) Зарегистрируйтесь как **преподаватель** (`teacher@test.com`) и проверьте задания.

---

## Зависимости (libraries/versions)

Файл `requirements.txt` содержит список необходимых библиотек:

```txt
fastapi==0.115.0
uvicorn[standard]==0.31.0
jinja2==3.1.4
python-multipart==0.0.9
itsdangerous==2.2.0
sqlalchemy==2.0.31
# sqlite3 входит в стандартную библиотеку Python

## MilnSep состав команды

- Калюжин Глеб Валерьевич - frontend-разработчик, тимлид
- Косяшников Никита Сергеевич - backend-разработчик
- Горячевский Алексей Романович - backend-разработчик
- Гулый Михаил Александрович - data scientist
- Шевченко Тихон Владимирович - frontend-разработчик, UI-UX дизайнер